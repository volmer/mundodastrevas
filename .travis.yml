language: ruby
rvm:
  - 2.2.4
before_script:
  - 'export DISPLAY=:99.0'
  - 'sh -e /etc/init.d/xvfb start'
  - bundle exec rake db:setup
bundler_args: '--without development production'
services:
  - elasticsearch
