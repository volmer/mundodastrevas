<li class="media activity">
  <%= link_to image_tag(activity.user.avatar.thumb.url, class: 'img-circle'), activity.user, class: 'media-left' %>

  <div class="media-body">
    <%= activity_content(activity) %>

    <%= time_tag activity.created_at, format: :short %>
  </div>
</li>
