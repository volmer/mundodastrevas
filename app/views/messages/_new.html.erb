<%= simple_form_for(Message.new, url: user_messages_path(@user)) do |f| %>
  <%= f.input :content, autofocus: true, label: false, input_html: { rows: 5 } %>

  <%= f.button :submit, t('.submit') %>
<% end %>
