<%= render 'author', user: @zine.user %>

<% if policy(@zine.posts.new).create? %>
  <section class="panel panel-default">
    <div class="panel-body">
      <%= link_to t('.new_post'), new_zine_post_path(@zine), class: 'btn btn-primary btn-block' %>
    </div>
  </section>
<% end %>

<%= render 'posts/panel', zine: @zine %>
